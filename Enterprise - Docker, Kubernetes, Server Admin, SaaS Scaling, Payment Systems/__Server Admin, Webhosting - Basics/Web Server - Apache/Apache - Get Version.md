
METHOD A: Ppen php file that contains:
```
phpinfo();
```

Then look for, like example:

| Apache Version | Apache/2.4.54 (Unix) |
|-----------------|----------------------|

---

METHOD B:

Run command
```
apache2 -v
```