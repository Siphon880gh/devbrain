
Claude Code is a command-line developer tool rather than a standalone API service. However, it operates on Anthropic’s underlying Claude API:
https://platform.claude.com/docs/en/api/overview

---

Per: https://code.claude.com/docs/en/quickstart

Mac installations:
```
curl -fsSL https://claude.ai/install.sh | bash
```