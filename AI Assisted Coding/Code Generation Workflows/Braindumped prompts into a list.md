Braindumped a bunch of prompts into a list?
  
Prompt:
```
Review this list of prompts for the current project and figure out a logical implementation order. Place prerequisites before the tasks that depend on them.

Then start implementing them. Perform autoverification to check an implementation works. Only stop if need human verification. Make a git commit after each successful implementation and autoverification.

List of prompts:
{Place your prompts here}
```


> [!note] Example Prompt
>Review this list of prompts for the current project and figure out a logical implementation order. Place prerequisites before the tasks that depend on them.
>
> Then start implementing them. Perform autoverification to check an implementation works. Only stop if need human verification. Make a git commit after each successful implementation and autoverification.
> 
> List of prompts:
> - Move that dropdown of projects to the left of New Project. We can remove Narration Settings that’s next to New Project since we have that setting inside the Player screen already.
> - When reading out loud the slip name, skip reading underscore (_), dashes, etc
> - Audio should stop playing in the background when user leaves the Player (eg. Goes to Annotate) or switching to another Project or playable workflow
> - At Annotate screen: Add a Circle tool next to Box
> - Narration settings button should have another icon, preferably something with sliders
> - Next to Export menu option, add Sync. Under Sync the only option is to Sync to Demo with a warning that this will make it appear to all current users.
> - Set aside visually Export and Sync. Maybe we add a vertical divider?
>

