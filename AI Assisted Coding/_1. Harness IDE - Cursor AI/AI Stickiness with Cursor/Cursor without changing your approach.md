Find it kept changing your approach? Undo or git reset back to your code, then tell it to not change your approach.

Then **prompt**:
{DESCRIBE PROBLEM}. Fix without changing my approach of {DESCRIBE APPROACHE(S)}.