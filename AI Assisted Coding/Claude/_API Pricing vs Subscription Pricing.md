Note: Here **custom use** means using their API for your own development, apps, automations, or integrations.

For **Claude**, custom use could involve the **Claude API**, which is billed separately through API top-ups or pay-as-you-go billing. If you are using a Claude-owned product directly, then a Claude subscription may apply. For example, on Claude Code you can go with either paths: use your Pro/Max plan allocation, or choose API credits billed at standard API rates.

But if you are using Claude through a third-party tool like Cursor, then access is usually handled through that tool’s billing or through your own API key.