See here:
```
# Check if the local machine is reachable (basic connectivity test)
ping localhost

# Check if can connect to internet (Note google.com's IP is 142.251.40.46)
ping google.com
ping 142.251.40.46

# Test if a service is listening on port 8080 of the local machine
telnet localhost 8080
```