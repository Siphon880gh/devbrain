If you have a lot of source documents, you can use Google NotebookLM

Another option is to use an IDE like Cursor. You can actually inform Cursor that it's a different role, eg. "You are a SEO professional".

You can also use a cli tooled agent like command `claude` or command `cursor-agent`. It's designed to handle numerous files.