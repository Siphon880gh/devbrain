# GitHub Actions for AWS Deployments

GitHub Actions is a CI/CD platform built into GitHub. If your code already lives on GitHub, Actions lets you build and deploy to AWS without leaving the GitHub ecosystem. You define workflows in YAML files, and they run automatically on events like pushes, pull requests, or scheduled times.

## What You'll Learn

- Why GitHub Actions is a popular alternative to CodePipeline
- How to securely configure AWS credentials using OIDC (recommended) or access keys
- How to write workflow files for common AWS deployments
- Deployment workflows for S3, Lambda, ECS/Fargate, and CDK
- Environment protection rules and secrets management
- How GitHub Actions compares to AWS CodePipeline

## Why GitHub Actions for AWS

GitHub Actions vs AWS CodePipeline — both build CI/CD pipelines, but they have different strengths:

| Feature | GitHub Actions | CodePipeline |
|---------|---------------|--------------|
| Where it lives | GitHub | AWS Console |
| Configuration | YAML in your repo | Console or CLI/JSON |
| Trigger sources | GitHub events (push, PR, schedule, manual) | GitHub, CodeCommit, S3 |
| Runner options | GitHub-hosted or self-hosted | CodeBuild managed containers |
| Marketplace | 20,000+ community actions | Limited to AWS services |
| Cost | Free tier: 2,000 min/month | Pay per pipeline + CodeBuild minutes |
| Multi-cloud | Deploy anywhere | AWS only |

**Choose GitHub Actions when:** Your code is on GitHub, you want to deploy to multiple clouds, or you prefer managing CI/CD alongside your code.

**Choose CodePipeline when:** You need deep AWS integration, blue/green deployments via CodeDeploy, or approval gates tied to AWS IAM.

## Configuring AWS Credentials in GitHub

### Option 1: OIDC (Recommended)

OpenID Connect (OIDC) lets GitHub Actions assume an AWS IAM role without storing long-lived access keys. GitHub proves its identity to AWS using a short-lived token.

**Step 1: Create an OIDC identity provider in AWS**

```bash
aws iam create-open-id-connect-provider \
  --url https://token.actions.githubusercontent.com \
  --client-id-list sts.amazonaws.com \
  --thumbprint-list 6938fd4d98bab03faadb97b34396831e3780aea1
```

**Step 2: Create an IAM role that GitHub can assume**

Create `github-actions-trust-policy.json`:

```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Principal": {
        "Federated": "arn:aws:iam::123456789012:oidc-provider/token.actions.githubusercontent.com"
      },
      "Action": "sts:AssumeRoleWithWebIdentity",
      "Condition": {
        "StringEquals": {
          "token.actions.githubusercontent.com:aud": "sts.amazonaws.com"
        },
        "StringLike": {
          "token.actions.githubusercontent.com:sub": "repo:myuser/myrepo:*"
        }
      }
    }
  ]
}
```

```bash
aws iam create-role \
  --role-name github-actions-deploy \
  --assume-role-policy-document file://github-actions-trust-policy.json

aws iam attach-role-policy \
  --role-name github-actions-deploy \
  --policy-arn arn:aws:iam::aws:policy/AdministratorAccess
```

In production, use a more restrictive policy than `AdministratorAccess`. Scope it to only the services your deployment needs.

**Step 3: Use in your workflow**

```yaml
permissions:
  id-token: write
  contents: read

steps:
  - name: Configure AWS credentials
    uses: aws-actions/configure-aws-credentials@v4
    with:
      role-to-assume: arn:aws:iam::123456789012:role/github-actions-deploy
      aws-region: us-east-1
```

### Option 2: Access Keys (Simpler but Less Secure)

Store your access key and secret key as GitHub secrets:

1. Go to your repo → **Settings** → **Secrets and variables** → **Actions**
2. Add `AWS_ACCESS_KEY_ID` and `AWS_SECRET_ACCESS_KEY`

Use in your workflow:

```yaml
steps:
  - name: Configure AWS credentials
    uses: aws-actions/configure-aws-credentials@v4
    with:
      aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
      aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      aws-region: us-east-1
```

OIDC is preferred because there are no long-lived secrets to rotate or leak.

## Workflow File Anatomy

GitHub Actions workflows live in `.github/workflows/` as YAML files. Here's the basic structure:

```yaml
name: Deploy to AWS

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::123456789012:role/github-actions-deploy
          aws-region: us-east-1

      - name: Do something with AWS
        run: aws s3 ls
```

**on** — The event that triggers the workflow. Can be `push`, `pull_request`, `schedule`, `workflow_dispatch` (manual trigger), and more.

**jobs** — One or more jobs that run in parallel by default. Each job runs on a fresh virtual machine.

**steps** — Sequential actions within a job. Each step is either a community action (`uses`) or a shell command (`run`).

## Deploy to S3 (Static Website)

```yaml
name: Deploy Static Site to S3

on:
  push:
    branches: [main]

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test -- --watchAll=false

      - name: Build
        run: npm run build

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::123456789012:role/github-actions-deploy
          aws-region: us-east-1

      - name: Deploy to S3
        run: aws s3 sync ./build s3://my-website-bucket --delete

      - name: Invalidate CloudFront cache
        run: |
          aws cloudfront create-invalidation \
            --distribution-id E1A2B3C4D5E6F7 \
            --paths "/*"
```

## Deploy a Lambda Function

```yaml
name: Deploy Lambda Function

on:
  push:
    branches: [main]
    paths:
      - "src/lambda/**"

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          cd src/lambda
          pip install -r requirements.txt -t ./package
          cd package && zip -r ../../function.zip .
          cd .. && zip -g ../function.zip handler.py

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::123456789012:role/github-actions-deploy
          aws-region: us-east-1

      - name: Deploy to Lambda
        run: |
          aws lambda update-function-code \
            --function-name my-processor \
            --zip-file fileb://function.zip
```

The `paths` filter means this workflow only triggers when files under `src/lambda/` change — no point redeploying the Lambda when you only changed the README.

## Deploy to ECS/Fargate

```yaml
name: Deploy to ECS Fargate

on:
  push:
    branches: [main]

permissions:
  id-token: write
  contents: read

env:
  ECR_REPOSITORY: my-app
  ECS_SERVICE: my-app-service
  ECS_CLUSTER: my-cluster
  CONTAINER_NAME: my-app-container

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::123456789012:role/github-actions-deploy
          aws-region: us-east-1

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build, tag, and push Docker image
        id: build-image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          echo "image=$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" >> $GITHUB_OUTPUT

      - name: Download current task definition
        run: |
          aws ecs describe-task-definition \
            --task-definition my-app-task \
            --query taskDefinition > task-definition.json

      - name: Update task definition with new image
        id: task-def
        uses: aws-actions/amazon-ecs-render-task-definition@v1
        with:
          task-definition: task-definition.json
          container-name: ${{ env.CONTAINER_NAME }}
          image: ${{ steps.build-image.outputs.image }}

      - name: Deploy to ECS
        uses: aws-actions/amazon-ecs-deploy-task-definition@v2
        with:
          task-definition: ${{ steps.task-def.outputs.task-definition }}
          service: ${{ env.ECS_SERVICE }}
          cluster: ${{ env.ECS_CLUSTER }}
          wait-for-service-stability: true
```

This workflow builds a Docker image, pushes it to ECR, updates the ECS task definition with the new image tag, and deploys it. The `wait-for-service-stability` option makes the workflow wait until the new containers are healthy.

## Deploy a CDK Stack

```yaml
name: Deploy CDK Stack

on:
  push:
    branches: [main]

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run CDK tests
        run: npm test

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::123456789012:role/github-actions-deploy
          aws-region: us-east-1

      - name: CDK diff
        run: npx cdk diff

      - name: CDK deploy
        run: npx cdk deploy --all --require-approval never
```

The `--require-approval never` flag skips the interactive confirmation prompt, which is necessary in CI/CD since there's no human at the terminal.

## Environment Secrets and Protection Rules

### Environment-Specific Secrets

GitHub lets you create **environments** (like `staging` and `production`) with their own secrets and protection rules:

1. Go to **Settings** → **Environments** → **New environment**
2. Add environment-specific secrets (like different AWS role ARNs per environment)
3. Add protection rules (required reviewers, wait timers)

Use environments in your workflow:

```yaml
jobs:
  deploy-staging:
    runs-on: ubuntu-latest
    environment: staging
    steps:
      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: us-east-1

  deploy-production:
    runs-on: ubuntu-latest
    environment: production
    needs: deploy-staging
    steps:
      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: us-east-1
```

With protection rules on the `production` environment, a team member must approve the deployment before it runs — giving you the same "manual approval" stage that CodePipeline offers.

### Branch Protection

Combine environment protection with branch protection rules:
- Require pull request reviews before merging to `main`
- Require status checks (CI tests) to pass
- Require branches to be up to date

This means only reviewed, tested code reaches your deployment pipeline.

## Practical Tips

**Use `workflow_dispatch` for manual deploys:**

```yaml
on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Target environment"
        required: true
        type: choice
        options:
          - staging
          - production
```

**Cache dependencies for faster builds:**

```yaml
- uses: actions/setup-node@v4
  with:
    node-version: 20
    cache: npm
```

**Run tests on PRs, deploy only on push to main:**

```yaml
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci && npm test

  deploy:
    needs: test
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      # deploy steps here
```

**Use concurrency to prevent overlapping deploys:**

```yaml
concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true
```

## Key Takeaways

- GitHub Actions workflows live in `.github/workflows/` as YAML files alongside your code
- Use **OIDC** with `aws-actions/configure-aws-credentials` to securely authenticate — no long-lived access keys
- The `aws-actions/*` family of actions covers ECR login, ECS deployment, and task definition rendering
- Use **environments** with protection rules for production deploy gates
- Path filters (`paths: ["src/lambda/**"]`) prevent unnecessary deployments
- GitHub Actions is more flexible than CodePipeline (multi-cloud, huge marketplace) while CodePipeline has deeper AWS integration (blue/green via CodeDeploy, native CloudFormation actions)
- The `concurrency` setting prevents multiple deploys from running simultaneously

---

← Previous: [CodePipeline, CodeBuild, and CodeDeploy](5.%20CodePipeline%2C%20CodeBuild%2C%20and%20CodeDeploy.md) | Next: [CloudWatch Logs and Metrics](../08%20-%20Monitoring/1.%20CloudWatch%20Logs%20and%20Metrics.md) →
