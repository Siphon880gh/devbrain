
If the Merge node doesn’t return any output while testing the workflow:  **Save the workflow and refresh the page**, then run the Merge node again — the output should appear.

So far glitchy as of 1.115.3