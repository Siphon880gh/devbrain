ChatGPT will generate the wrong syntax for Puppeteer IDE due to syntax and version differences.

You can attempt to tune your model into the correct version by reminding it is the Chrome Extension version (even give it the specific version number), but it doesn't seem to work.

Instead, load up my tutorials on Puppeteer IDE in a tool like Cursor, then you can ask your questions there:
![[Pasted image 20260531023552.png]]

It may at first erroneously think that your file contents is what you want to scrape or use as a scraper rather than as tutorial source. It'll figure that out as it thinks longer on the first prompt.

This kicks the AI model into the correct version of knowledge so it can generate the correct Puppeteer IDE code.