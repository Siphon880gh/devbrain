1. **Install:** `curl -fsSL https://tailscale.com/install.sh | sh`
2. **Tailscale Daemon Background Service should automatically have started.** Test with: `sudo systemctl status tailscaled`

3. Run `tailscale up` then visit URL outputted in order to authenticate
![[Pasted image 20260421210937.png]]

Choose your authentication method:
![[Pasted image 20260421211023.png]]

![[Pasted image 20260421211405.png]]


![[Pasted image 20260421211555.png]]

![[Pasted image 20260421211744.png]]

4. Run `tailscale status`:
```
100.80.255.1  wengs-macbook-pro-new  siphon880g@  macOS  -
```

This means I can ping this address too:
```
ping 100.80.255.1
```

For example if you have an express server.js running on the server side like:
(Initiate a NodeJS app with: `npm init -y` -> `npm install express`)
(Then run with `node server.js`)
File `server.js`:
```
const express = require('express');
const app = express();
const PORT = 3000;

// Root route
app.get('/', (req, res) => {
  res.send('Express works');
});

// Start server
app.listen(PORT, () => {
  console.log(`Server running on http://localhost:${PORT}`);
});
```

As a quick test, I run it in foreground at a ssh
![[Pasted image 20260420064652.png]]

Curl the webpage to see it working:
![[Pasted image 20260421230731.png]]

---

Lets make sure you can change ip and test it really works.

You should see this page at
https://login.tailscale.com/admin/machines
Then go Edit machine IP
![[Pasted image 20260421220449.png]]
> [!note] Don't see that page?
> If instead you see a different page, complete the survey, then skip the introduction at the footer here:
> ![[Pasted image 20260421220052.png]]
>

Increment your IP, like change 100.80.255.1 to 100.80.255.2. See if the same port is opened

Curl the webpage to see it working:
![[Pasted image 20260421230506.png]]

Visiting with no port (defaults to 80 or 443) would have nothing to present if localhost has nothing to present:
http://100.80.255.2/

That's fine.