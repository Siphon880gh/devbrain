# ES6 Fundamental: Spread operator

Spread the array elements into comma separated variables
```
var arr = ["a","b","c"]
function showArgs(first, second, third) {
    console.log(first);
    console.log(second);
    console.log(third);
};
showArgs(arr);
```