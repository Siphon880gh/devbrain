Run `hostname && whoami`
It outputs username and the server name

Eg.
```
remote-server-123
root
```

This is useful if you manage multiple servers' SSH in the terminal. You can quickly reorientate to a tab.