To find out the architecture you’re on, run the command:
```
uname -m
```

Interpretation:
- `x86_64` → **64-bit (Intel/AMD)**
- `i386`, `i686` → **32-bit (Intel/AMD)**
- `aarch64` → **ARM64**
- `armv7l`, `armv6l` → **ARM32**