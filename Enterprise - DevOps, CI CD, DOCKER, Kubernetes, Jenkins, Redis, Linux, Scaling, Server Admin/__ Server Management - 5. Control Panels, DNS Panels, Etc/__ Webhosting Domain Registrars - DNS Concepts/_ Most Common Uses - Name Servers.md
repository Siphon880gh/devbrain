If you want purchased a domain at a DNS Registrar, but want to manage it at a different DNS registrar, you'd assign name servers.

For example, if you want Cloudflare to take over Namecheap's DNS management, you set the Cloudflare nameservers into Namecheap.

It's usually two addresses that looks like ns1.example.com and ns2.example.com