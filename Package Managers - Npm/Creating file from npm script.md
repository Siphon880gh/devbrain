You can have a npm script like:
```
"generate": "echo 'Test' > test.txt"
```

And you run
```
npm run generate
```

Then when you list files, you'll see the new "test.txt" file and it contains the line "Test"